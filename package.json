{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "nuxi generate && gh-pages -d .output/public"

  },
  "dependencies": {
    "@supabase/supabase-js": "^2.50.0",
    "@toast-ui/editor": "^3.2.2",
    "nuxt": "^3.17.5",
    "vue": "^3.5.16",
    "vue-router": "^4.5.1"
  }
}
